// src/screens/workouts/start-workout/styles/startWorkoutStyles.ts

import { StyleSheet } from "react-native";
import { colors } from "../../../../theme/colors";

export const startWorkoutStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#0a0a0a",
  },
  gradient: {
    position: "absolute",
    left: 0,
    right: 0,
    top: 0,
    bottom: 0,
  },
  // Add all the styles from the main component here
  // This helps keep the main component cleaner
});
