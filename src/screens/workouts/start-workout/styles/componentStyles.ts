// src/screens/workouts/start-workout/styles/componentStyles.ts

import { StyleSheet } from "react-native";
import { colors } from "../../../../theme/colors";

export const planCardStyles = StyleSheet.create({
  // PlanCard specific styles
});

export const dayCardStyles = StyleSheet.create({
  // DayCard specific styles
});

export const recentWorkoutStyles = StyleSheet.create({
  // RecentWorkouts specific styles
});

export const suggestionStyles = StyleSheet.create({
  // WorkoutSuggestion specific styles
});
